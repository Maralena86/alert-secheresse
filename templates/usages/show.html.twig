{% extends 'base.html.twig' %}

{% block title %}Usages{% endblock %}

{% block body %}
    <h1>Usages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ usage.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ usage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_usages_index') }}">back to list</a>

    <a href="{{ path('app_usages_edit', {'id': usage.id}) }}">edit</a>

    {{ include('usages/_delete_form.html.twig') }}
{% endblock %}
